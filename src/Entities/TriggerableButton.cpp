//
// Created by micha on 20.06.2020.
//

#include "TriggerableButton.h"


void TriggerableButton::update(float deltaTime) {
    Entity::update(deltaTime);
}

void TriggerableButton::draw(sf::RenderWindow &window) {
    Entity::draw(window);
}
